Read the `about` file. Pick one book to give each person as a holiday gift,
plus one for me to read myself. Write the picks (one or two lines of
reasoning per person) to a file called `suggestions`. Improve `suggestions`
if it already exists — keep what works, swap what does not.
